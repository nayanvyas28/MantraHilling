{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "buildCommand": "cd web-admin && npm ci && npm run build",
    "startCommand": "cd web-admin && npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
